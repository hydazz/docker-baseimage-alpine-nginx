#!/usr/bin/with-contenv bash

# nginx

echo "This may take a while, be patient!"
find /config -type d -exec chmod 0755 {} \; &
find /config -type f -exec chmod 0644 {} \;

chmod 0600 /config/keys/cert.key

echo "Please restart the container"
